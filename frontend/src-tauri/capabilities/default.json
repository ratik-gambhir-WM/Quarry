{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the bundled Quarry application",
  "windows": ["main"],
  "permissions": ["core:default"]
}
